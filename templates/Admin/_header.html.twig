<nav
    class="navbar navbar-expand-lg navbar-light bg-white distributor-nav border-bottom"
    style="box-shadow: -9px -1px 8px 0;"
>
        <div class="container-fluid">
            <div class="navbar-brand" id="brand">

            </div>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#main_nav"  aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="main_nav">
                <div class="navbar nav">
                    <img src="{{ asset('images/logo.png') }}" class="img-fluid" style="max-height: 60px">
                </div>
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item dropdown">
                        <a class="nav-link  dropdown-toggle" href="" data-bs-toggle="dropdown">
                            {{ render(controller(
                                'App\\Controller\\AdminDashboardController::sideBar', { 'return':'user' }
                            )) }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li>
                                <a class="dropdown-item" href="{{ path('admin_logout') }}">
                                    <i class="fa-solid fa-ban"></i>&nbsp;&nbsp;Sign Out
                                </a>
                            </li>
                        </ul>
                    </li>
                </ul>
            </div> <!-- navbar-collapse.// -->
        </div> <!-- container-fluid.// -->
    </nav>