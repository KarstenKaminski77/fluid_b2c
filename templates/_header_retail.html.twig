<header>
    <nav class="navbar navbar-expand-lg navbar-light bg-white fixed-top border-bottom">
        <div class="container-fluid">
            <button class="navbar-toggler" type="button" data-action="click->retail#onClickToggleNav">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="navbar-brand">
                <a href="{{ path('retail_search', {'pageNo': 1}) }}">
                    <img src="{{ clinicLogo }}" class="img-fluid text-center pe-3 pe-sm-0" style="max-height: 60px">
                </a>
            </div>
            <div class="collapse navbar-collapse" id="main_nav">
                <div class="navbar-nav mx-0 mx-md-auto w-sm-100-md-50">
                    <form
                        name="search_form"
                        class="w-100"
                        id="search_form"
                        method="post"
                    >
                        <ul class="navbar-nav">
                            <li class="nav-item" style="flex-grow: 100">
                                <div class="input-group mt-3 mt-md-0 pe-2">
                                    <input type="text" id="search_field" class="form-control form-control-bg-grey" placeholder="Search by item, sku, manufacturer, ingredient, size..." />
                                    <button
                                        type="submit"
                                        class="btn btn-secondary btn-sm"
                                        id="search_btn"
                                        data-action="click->retail-search#onSubmit"
                                    >
                                        <i class="fa-solid fa-magnifying-glass"></i>
                                    </button>
                                </div>
                            </li>
                        </ul>
                    </form>
                </div>
                <ul class="navbar-nav">
                    <li class="nav-item pt-1 pb-1 pt-sm-0 pb-sm-0 text-truncate">
                        <a
                            class="nav-link basket-link"
                            data-basket-id=""
                            id="btn_basket"
                            href="#"
                            data-action="click->retail-basket#onBasketClick click->retail#onClickToggleNav"
                        >
                            <i class="fa-light fa-basket-shopping pe-2 fa-width text-primary"></i>
                            <span class="d-sm-inline-block d-sm-none d-md-inline-block">Basket</span>
                        </a>
                    </li>
                    <li class="nav-item position-relative pt-1 pb-1 pt-sm-0 pb-sm-0">
                        <a class="nav-link btn-notifications text-truncate" name="notifications" href="#">
                            <i class="fa-light fa-bell pe-2 fa-width" id="notifications_icon"></i>
                            <span class="">Notifications</span>
                        </a>
                        <span id="notifications_panel" class="position-absolute end-0">
                        <span class="notification-panel delete-notification">You have no notifications</span>
                    </span>
                    </li>
                    <li class="nav-item dropdown">
                        <a class="nav-link  dropdown-toggle" href="{{ path('retail_logout') }}" data-bs-toggle="dropdown">
                            <i class="fa-light fa-user pe-2 fa-width text-primary"></i>
                            {{ nzo_decrypt(app.user.firstName) ~' '~ nzo_decrypt(app.user.lastName) }}
                        </a>
                        <ul class="dropdown-menu dropdown-menu-end">
                            <li>
                                <a class="dropdown-item" href="{{ path('retail_logout') }}">
                                    <i class="fa-solid fa-ban"></i>&nbsp;&nbsp;Sign Out
                                </a>
                            </li>
                        </ul>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>